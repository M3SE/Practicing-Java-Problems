public boolean makes10(int a, int b) {
    return (a == 10 || b == 10 || a+b == 10);
}

/* The method takes two integer parameters a and b
it returns true if either a is 10, b is 10, or the sum of a and b is 10
if none of these conditions are met, it returns false. */